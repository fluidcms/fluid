require(['bootstrap'], function () {
    require(['backbone'], function (Backbone) {
        var Module = Backbone.Router.extend({
            root: window.location.pathname,

            initialize: function () {
            },

            routes: {
                "": "index"
            },

            index: function () {
                alert();
            }
        });

        new Module();
        Backbone.history.start({pushState: true, root: Module.root});
    });
});